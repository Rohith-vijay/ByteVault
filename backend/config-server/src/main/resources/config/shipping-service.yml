server:
  port: 8089

spring:
  datasource:
    url: ${SHIPPING_DB_URL:jdbc:postgresql://localhost:5489/shipping_db}
    username: ${SHIPPING_DB_USER:admin}
    password: ${SHIPPING_DB_PASS:password}
  jpa:
    hibernate:
      ddl-auto: update
