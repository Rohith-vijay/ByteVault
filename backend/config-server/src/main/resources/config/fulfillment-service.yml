server:
  port: 8088

spring:
  datasource:
    url: ${FULFILLMENT_DB_URL:jdbc:postgresql://localhost:5438/fulfillment_db}
    username: ${FULFILLMENT_DB_USER:admin}
    password: ${FULFILLMENT_DB_PASS:password}
  jpa:
    hibernate:
      ddl-auto: update
