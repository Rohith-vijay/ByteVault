server:
  port: 8081

spring:
  datasource:
    url: ${AUTH_DB_URL:jdbc:postgresql://localhost:5432/auth_db}
    username: ${AUTH_DB_USER:admin}
    password: ${AUTH_DB_PASS:password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

jwt:
  refresh-expiration: 604800000 # 7 days
