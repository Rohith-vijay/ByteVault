server:
  port: 8082

spring:
  datasource:
    url: ${USER_DB_URL:jdbc:postgresql://localhost:5433/user_db}
    username: ${USER_DB_USER:admin}
    password: ${USER_DB_PASS:password}
  jpa:
    hibernate:
      ddl-auto: update
